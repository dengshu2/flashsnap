services:
  flashsnap:
    build:
      context: .
      args:
        VITE_UMAMI_WEBSITE_ID: ${VITE_UMAMI_WEBSITE_ID}
    container_name: flashsnap
    restart: unless-stopped
    networks:
      - npm-network

networks:
  npm-network:
    external: true
